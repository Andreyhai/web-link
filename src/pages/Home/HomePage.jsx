import React, { Fragment } from 'react';

import Header from '../../components/header/Header';

const HomePage = () => {
    return (
        <Fragment>
            <Header />
        </Fragment>
    );
};

export default HomePage;